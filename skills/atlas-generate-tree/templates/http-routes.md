# HTTP Routes Catalog — <Repo Name>

> Flat table of every HTTP route exposed by this repo. Created ONLY when the repo exposes ≥10 routes spread across ≥3 features. Alphabetical by `<METHOD> <path>`.

## Routes

- `GET    /<path>`         — <one-line purpose> → [<feature-name>](../<feature-name>/feature.md)
- `POST   /<path>`         — <one-line purpose> → [<feature-name>](../<feature-name>/feature.md)
- `PUT    /<path>/:id`     — <one-line purpose> → [<feature-name>](../<feature-name>/feature.md)
- `DELETE /<path>/:id`     — <one-line purpose> → [<feature-name>](../<feature-name>/feature.md)
