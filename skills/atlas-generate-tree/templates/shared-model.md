# Model: <ModelName>

> Cross-feature shared-model leaf. ≤80 lines. Created ONLY when ≥3 features reference this model.

## Breadcrumb

[<repo-name>](../../ATLAS.md) → [Shared Models](../INDEX.md#shared-models) → <ModelName>

## Defined In

<file>:<line> — <package or module>

## Shape

<!-- Canonical shape. Code excerpt ≤30 lines. -->

`<file>:<line>`

```<lang>
<excerpted struct / class / schema>
```

## Used By

<!-- ≥3 features. Each bullet links to a feature leaf and notes how the model is used. -->

- [<feature-name>](../<feature-name>/feature.md) — <reads | writes | both>
- [<feature-name>](../<feature-name>/feature.md) — <reads | writes | both>
- [<feature-name>](../<feature-name>/feature.md) — <reads | writes | both>

## Persistence

<!-- One paragraph: where instances are stored, key columns/fields, lifetime. Omit body and write `_Not persisted._` if the model is in-memory only. -->

<paragraph>

## Last Verified

<commit-sha-short> (<YYYY-MM-DD>)
