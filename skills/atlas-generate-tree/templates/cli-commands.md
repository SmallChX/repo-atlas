# CLI Commands Catalog — <Repo Name>

> Flat table of every CLI subcommand exposed by this repo. Created ONLY when the repo exposes ≥10 subcommands. Alphabetical by command path.

## Commands

- `<bin> <subcommand>`              — <one-line purpose> → [<feature-name>](../<feature-name>/feature.md)
- `<bin> <group> <subcommand>`      — <one-line purpose> → [<feature-name>](../<feature-name>/feature.md)
